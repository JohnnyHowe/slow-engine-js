

/** Given a position on the screen, get the corresponding game position.
 * @param {Geometry.Vector} screenPos - Position (in pixels) to be converted.
 * @returns {Geometry.Vector} gamePos - position in game units.
 */
function getGamePos(screenPos) {
    
}