
/** Draw a solid color rect centered on position.
 * Position and size measured in game units.
 * @param {Geometry.Vector} position - centre position of rect to draw
 * @param {Geometry.Vector} size - size of rect to draw
 */
function drawGameRect(position, size, color) {

}


/** Draw a solid color rect centered on position.
 * Position and size measured in pixels.
 * @param {Geometry.Vector} position - centre position of rect to draw
 * @param {Geometry.Vector} size - size of rect to draw
 */
function drawScreenRect(engine, position, size, color) {

}


export {
    drawGameRect,
    drawScreenRect,
}